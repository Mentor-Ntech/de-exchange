import './WaitList.css';

const WaitList = () => {
  return (
    <div className="--dir-column --align-center waitList">
      <p>Be the first to get updated, Join the Movement.</p>
      <div>

      <button>Join Waitlist 😋</button>
      </div>
    </div>
  );
};

export default WaitList;
